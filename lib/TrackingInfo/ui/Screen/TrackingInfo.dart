import 'package:flutter/material.dart';
class TrackingInfo extends StatefulWidget {
  const TrackingInfo({Key? key}) : super(key: key);

  @override
  State<TrackingInfo> createState() => _TrackingInfoState();
}

class _TrackingInfoState extends State<TrackingInfo> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
